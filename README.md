# GTI-3B-SBOCK
Proyecto de robótica - 3º GTI


si no funciona el .gitignore poner
git rm -r --cached .


Para iniciar la web primero hacemos:

    $ npm install

lugo iniciamos node:

    $ npm run dev
